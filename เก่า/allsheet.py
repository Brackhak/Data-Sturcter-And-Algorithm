a = [] #list  a[-1] คือตัวท้ายสุด // .append("__") .len() .insert(1,"___") .remove("__") .pop()
    # del a[0] or del a // .clear()

b = () #tuple

c = {} #set พิมพ์สุ่ม .add("") .update("") .remove("") .discard("") .pop() .clear() // del c

text = "brackhak" # .len() .strip() .lower() .upper() .replace("","") .split()

class myclass :
    def __init__(self,_,_) : #สามารถกำหนดค่าของตัวแปรไว้ให้ก่อนได้ เผื่อถ้าไม่ได้กำหนดจะกำหนดให้เลย
        self._ = _
        self._ = _
    
    def __str__(self):
        return str(__)

    